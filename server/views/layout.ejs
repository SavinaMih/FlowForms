<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link your stylesheet here -->
</head>
<body>
<header>
    <nav>
        <% if (user) { %>
            <p>Welcome, <%= user.name %>!</p>
            <a href="/auth/logout">Logout</a>
        <% } else { %>
            <a href="/auth/login">Login</a>
            <a href="/auth/signup">Sign Up</a>
        <% } %>
    </nav>
</header>

<!-- Flash Messages for Success and Error -->
<% if (success_msg) { %>
    <div class="flash success"><%= success_msg %></div>
<% } %>
<% if (error_msg) { %>
    <div class="flash error"><%= error_msg %></div>
<% } %>

<main>
    <%- body %> <!-- This will be replaced by each page’s main content -->
</main>

<footer>
    <p>&copy; 2023 FormFlow</p>
</footer>
</body>
</html>
